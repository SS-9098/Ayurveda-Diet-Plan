version: "3.8"

services:
  web:
    build: .
    image: shubhk2/ayushmitra:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./:/app
      - output:/app/output

volumes:
  output:
